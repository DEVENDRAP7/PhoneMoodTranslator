<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginHorizontal="20dp"
    android:layout_marginVertical="32dp"
    android:background="@drawable/bg_card_surface"
    android:padding="24dp">

<TextView
    android:id="@+id/tvCardEmoji"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="🤯"
    android:textSize="110sp"
    android:shadowColor="#40000000"
    android:shadowDx="0"
    android:shadowDy="12"
    android:shadowRadius="16"
    app:layout_constraintVertical_bias="0.35"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

<TextView
    android:id="@+id/tvCardTitle"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Overdose"
    android:textSize="28sp"
    android:textStyle="bold"
    android:textColor="#FFFFFF"
    android:letterSpacing="0.05"
    android:layout_marginTop="16dp"
    app:layout_constraintTop_toBottomOf="@id/tvCardEmoji"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

<TextView
    android:id="@+id/tvCardCriteria"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Usage > 5 Hours"
    android:textSize="14sp"
    android:textStyle="bold"
    android:textColor="#FFD700"
    android:background="@drawable/bg_criteria_pill"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp"
    android:layout_marginTop="12dp"
    app:layout_constraintTop_toBottomOf="@id/tvCardTitle"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

<TextView
    android:id="@+id/tvCardDesc"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:text="System overload detected. You've lived more online than offline today."
    android:textSize="15sp"
    android:textColor="#CCFFFFFF"
    android:gravity="center"
    android:lineSpacingExtra="5dp"
    android:layout_marginBottom="32dp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintWidth_percent="0.9"/>

</androidx.constraintlayout.widget.ConstraintLayout>